[project]
name = "reduced"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "delta-method>=0.1",
    "jupyterlab>=4.5.1",
    "scikit-hep>=2025.11.1",
]
